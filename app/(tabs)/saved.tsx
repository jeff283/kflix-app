import React from 'react'
import { Text, View } from 'react-native'

const saved = () => {
  return (
    <View
    className="flex-1 items-center justify-center "
     >
       <Text className="font-semibold text-3xl text-primary">Saved</Text>
     </View>
  )
}

export default saved